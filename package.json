{
  "name": "rmanager-core",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "rmanager-web",
    "rmanager-shared",
    "rmanager-api"
  ],
  "scripts": {
    "dev": "docker compose up postgres -d && concurrently \"npm run dev --workspace=rmanager-api\"  \"npm run dev --workspace=rmanager-web\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.8.3"
  }
}
