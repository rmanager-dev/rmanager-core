{
  "name": "rmanager-core",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "rmanager-web",
    "rmanager-shared"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=rmanager-web",
    "build": "npm run build --workspace=rmanager-web",
    "test": "npm run test --workspace=rmanager-shared"
  }
}
